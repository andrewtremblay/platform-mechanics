"""Contains all parseable settings for debug activities."""


class DebugConfig:
    """Contain all debug vars."""

    ENABLED = True
    # depending on how we want to log, this class can get complicated
    LOG_ENABLED = True

DEBUG = DebugConfig()
